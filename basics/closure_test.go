package basics

import (
	"fmt"
	"strings"
	"testing"
)

/*
Q1: 什么是闭包？
Q2: 闭包引用的变量存在哪？（底层原理）
Q3: 循环里创建闭包打印循环变量 i，为什么结果全是最大值？
Q4: 闭包的底层数据结构是什么？
Q5: 闭包会导致内存泄漏吗？什么场景下会？
Q6: 闭包和普通函数传参有什么区别？
Q7: 闭包有哪些实际应用场景？

---
Q1: 什么是闭包？
【理解】
闭包 = 函数 + 它引用的外部变量的绑定关系。
普通函数执行完局部变量就销毁了；闭包会把外层函数的变量"打包带走"，只要闭包还活着，这些变量就不会被 GC。
本质：函数"捕获"了外层变量的地址（不是值的拷贝），形成一个持久的引用关系。
【回答】
闭包就是一个函数加上它引用的外部变量。
可以把它想象成一个自带"私人背包"的函数。普通函数执行完，里面的局部变量就销毁了；但闭包不同，它会把定义时捕获的那些变量一直"背"在身上。只要这个闭包函数还在，它背包里的变量就一直活着，哪怕外层函数已经退出了。

---
Q2: 闭包引用的变量存在哪？（底层原理）
【理解】
涉及 Go 的逃逸分析（escape analysis）。
正常情况：函数局部变量在栈上，函数返回即回收。
闭包场景：编译器发现变量被闭包引用，函数返回后还要用，就把它"逃逸"到堆上。
编译器做了什么：
  1. 把被捕获的变量从栈上移到堆上分配
  2. 闭包结构体里存的是指向堆变量的指针
  3. 多个闭包捕获同一个变量时，它们共享同一个堆地址

验证方法：go build -gcflags="-m" 可以看到逃逸分析结果，会显示 "moved to heap"。
【回答】
这涉及到 Go 的逃逸分析。正常情况下，函数局部变量在栈上，函数结束就回收。但如果一个变量被闭包引用了，编译器发现它在函数返回后还要用，就会把它逃逸到堆上。
所以闭包底层其实是一个结构体，里面存了一个函数指针，还有一堆指向堆变量的指针。多个闭包捕获同一个变量时，它们共享同一个堆地址，这也是循环闭包 bug 的根源。

---
Q3: 循环里创建闭包打印循环变量 i，为什么结果全是最大值？
【理解】
根本原因：闭包捕获的是变量的地址，不是值的快照。
循环中所有闭包都指向同一个 i 的堆地址。循环跑完后 i=最大值，闭包执行时去这个地址读，自然全是最大值。

错误写法：
  for i := 0; i < 3; i++ {
      funcs = append(funcs, func() { fmt.Println(i) })  // 全部指向同一个 &i
  }
  // 输出：3 3 3

解决方法：
  方法 A（Go 1.22 之前）：i := i 创建局部副本，每次循环产生新地址
  方法 B：把 i 作为函数参数传入，利用值拷贝
  方法 C（Go 1.22+）：循环变量语义改了，每次迭代自动创建新变量，不再有这个坑

【回答】
因为闭包捕获的是变量的地址，而不是当时的值。
循环里所有闭包都指向同一个 i 的地址。循环跑得飞快，等闭包真正执行时循环早就结束了，此时 i 已经是最大值，所以全部输出相同。
解决方法：Go 1.22 之前用 i := i 创建副本或把 i 当参数传入；Go 1.22+ 循环变量语义已修改，每次迭代自动是新变量。

---
Q4: 闭包的底层数据结构是什么？
【理解】
编译器会为闭包生成一个 funcval 结构体：
  type funcval struct {
      fn uintptr    // 函数指针（指向闭包函数的代码）
  }
实际上被捕获的变量指针紧跟在 funcval 后面，内存布局类似：
  type closureStruct struct {
      fn     uintptr  // 函数指针
      &var1  *int     // 捕获变量1的地址
      &var2  *string  // 捕获变量2的地址
  }

调用闭包时：通过 funcval 指针找到函数代码，同时通过偏移量访问捕获的变量指针。
没有捕获变量的函数不会生成闭包结构体，就是普通函数调用。

【回答】
闭包底层是一个 funcval 结构体，包含一个函数指针和所有被捕获变量的指针。
编译器把被捕获变量逃逸到堆上，然后在 funcval 里存这些堆变量的地址。
调用闭包时，通过 funcval 既能找到函数代码，又能通过偏移量访问捕获的变量。
如果没有捕获任何外部变量，编译器不会生成闭包结构体，就是普通函数调用，没有额外开销。

---
Q5: 闭包会导致内存泄漏吗？什么场景下会？
【理解】
闭包本身不泄漏，但如果闭包被长期持有，它捕获的变量就无法被 GC：
  场景 A：闭包捕获了大 slice/map，且闭包被存到全局 map 或长生命周期对象里
  场景 B：闭包作为 goroutine 的函数体，goroutine 泄漏导致闭包永远不释放
  场景 C：闭包被注册为回调但永远不会被取消注册

解决方法：
  - 只捕获需要的字段，不要捕获整个大结构体
  - 用完后手动置空：bigSlice = nil
  - 传参替代捕获：把大对象当参数传入，用完即释放
【回答】
闭包本身不泄漏，但如果闭包被长期持有（比如存在全局 map 里、或者 goroutine 泄漏），它捕获的所有变量都无法被 GC 回收。
比如闭包捕获了一个很大的 slice，而这个闭包又被长期持有，那这个大 slice 就一直留在堆上。
解决方法：只捕获需要的字段、用完手动置空、或者改用传参替代闭包捕获。

---
Q6: 闭包和普通函数传参有什么区别？
【理解】
传参：值拷贝，函数内修改不影响外部，每次调用独立。
闭包捕获：共享地址，闭包内修改直接影响外部变量，多次调用之间状态持续。

本质区别：
  传参 -> 值语义，独立副本
  闭包 -> 引用语义，共享状态

这也是为什么闭包能实现"状态保持"（如计数器），而普通函数做不到。
【回答】
传参是值拷贝，函数内修改不影响外部，每次调用都是独立的。
闭包捕获是共享地址，闭包内修改直接影响外部变量，多次调用之间状态是持续的。
这就是为什么闭包能实现计数器这种"有记忆"的函数，而普通函数每次调用都是从零开始。

---
Q7: 闭包有哪些实际应用场景？
【理解】
1. 状态保持/计数器：函数调用之间维持状态，不需要全局变量
2. 函数工厂：根据参数生成定制化函数（如后缀检查器、权限校验器）
3. 装饰器/中间件：给函数附加行为（如耗时统计、日志、重试）
4. 延迟执行/回调：defer、goroutine 函数体、sort.Slice 的 less 函数
5. 配置选项模式：functional options pattern（如 grpc.WithXxx）
【回答】
常见应用场景：
状态保持——比如计数器，多次调用之间维持状态不需要全局变量。
函数工厂——根据参数生成定制化函数，比如后缀检查器、权限校验器。
装饰器/中间件——给函数附加行为，比如耗时统计、日志记录、自动重试。
延迟执行——defer 和 goroutine 的函数体天然就是闭包。
配置选项模式——functional options pattern，如 grpc.WithTimeout() 这种写法。

*/

// TestClosure 闭包核心特性演示
func TestClosure(t *testing.T) {
	// 状态保持：每次调用 count 自增
	next := counter()
	fmt.Println(next()) // 1
	fmt.Println(next()) // 2
	fmt.Println(next()) // 3

	// 函数工厂：生成带 .log 后缀检查的函数
	addLog := makeSuffix(".log")
	fmt.Println(addLog("access"))    // access.log
	fmt.Println(addLog("error.log")) // error.log

	// 循环闭包陷阱
	closureLoop()
}

// 状态保持：count 逃逸到堆上，闭包每次调用共享同一个 count
func counter() func() int {
	count := 0
	return func() int {
		count++
		return count
	}
}

// 函数工厂：预配置好后缀逻辑，到处复用
func makeSuffix(suffix string) func(string) string {
	return func(name string) string {
		if !strings.HasSuffix(name, suffix) {
			return name + suffix
		}
		return name
	}
}

// 循环闭包陷阱：所有闭包共享同一个 i 的地址
func closureLoop() {
	var funcs []func()

	for i := 0; i < 3; i++ {
		// 错误：所有闭包指向同一个 &i，输出全是 3
		funcs = append(funcs, func() {
			fmt.Printf("错误 i: %d\n", i)
		})
	}
	for _, f := range funcs {
		f() // 3 3 3
	}

	// 修复：i := i 创建局部副本
	var funcs2 []func()
	for i := 0; i < 3; i++ {
		i := i // 每次循环创建新变量，新地址
		funcs2 = append(funcs2, func() {
			fmt.Printf("正确 i: %d\n", i)
		})
	}
	for _, f := range funcs2 {
		f() // 0 1 2
	}
}
